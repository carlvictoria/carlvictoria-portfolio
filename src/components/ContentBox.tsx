export default function ContentBox() {
  return (
   <div className="mt-8 p-8 border border-gray-600 rounded-lg w-full max-w-4xl min-h-[400px]">
      <p style={{color: 'var(--foreground)', fontFamily: 'var(--font-terminal)'}} className="text-lg">
        Welcome to my portfolio
      </p>
    </div>
  );
}
